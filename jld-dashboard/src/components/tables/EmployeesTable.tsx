import type { Employee } from '../../types';
import { formatCurrency } from '../../utils/calculations';
import { RecordTable } from './RecordTable';
interface EmployeesTableProps{employees:Employee[];onAddEmployee?:()=>void;onArchiveEmployee?:(r:Employee)=>void;onRestoreEmployee?:(r:Employee)=>void;onPermanentDeleteEmployee?:(r:Employee)=>void;onViewHistory?:(r:Employee)=>void;}
export function EmployeesTable(p:EmployeesTableProps){return <RecordTable rows={p.employees} rowKey={r=>r.idemployee} searchText={r=>`${r.idemployee} ${r.fullname} ${r.designation} ${r.contactno}`} addLabel="Add employee" onAdd={p.onAddEmployee} actions={{onArchive:p.onArchiveEmployee,onRestore:p.onRestoreEmployee,onDelete:p.onPermanentDeleteEmployee,onHistory:p.onViewHistory}} columns={[{label:'Employee ID',render:r=>`EMP-${r.idemployee}`},{label:'Employee name',render:r=><strong>{r.fullname}</strong>},{label:'Designation',render:r=>r.designation||'—'},{label:'Contact number',render:r=>r.contactno||'—'},{label:'Gender',render:r=>r.gender||'—'},{label:'Civil status',render:r=>r.civilstatus||'—'},{label:'Daily rate (PHP)',numeric:true,render:r=>formatCurrency(r.salary)}]}/>}
